# Action Plan Standard

All model-generated behavior flows through parsing, validation, and execution stages defined in [SPEC.md](../../SPEC.md).

Unvalidated model output must never execute directly.

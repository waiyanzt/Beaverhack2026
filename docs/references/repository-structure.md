# Repository Structure

Top-level layout follows the structure defined in [SPEC.md](../../SPEC.md):

- `electron/` for the desktop app
- `apps/` for future apps
- `packages/` for shared packages
- `docs/` for standards and feature documentation
- `models/` for prompt and provider assets
- `scripts/` for repo-level automation
